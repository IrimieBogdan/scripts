- job-group:
    name: 'unit-rb_pe-razor-server'
    jobs:
        - '{value_stream}_pe-razor-server_unit-ruby_{qualifier}':
            dn-sort-index: ' 05:'
            qualifier: '{scm_branch}'
            tpb_projects: '{obj:downstream_jobs}'
