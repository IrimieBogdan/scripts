- job-group:
    name: 'clj-unit-release'
    jobs:
        - '{value_stream}_{name}_unit-clj_{qualifier}':
            qualifier: 'release_{scm_branch}'
            dn-qualifier: ' Pre-Release Clojure Unit Tests'
            dn-sort-index: ' Release-01:'
            tpb_projects: '{value_stream}_{name}_release-clj_{scm_branch}'

        - '{value_stream}_{name}_release-clj_{qualifier}':
            dn-sort-index: ' Release-05:'
            tpb_projects: '{value_stream}_{name}_pkg-pe_{scm_branch}'

- job-group:
    name: 'clj-pe-console-middleware-unit-release'
    jobs:
        - '{value_stream}_{name}_unit-clj_pe-console-middleware_{qualifier}':
            qualifier: 'release_{scm_branch}'
            dn-qualifier: ' Pre-Release Clojure Unit Tests'
            dn-sort-index: ' Release-01:'
            tpb_projects: '{value_stream}_{name}_release-clj_{scm_branch}'

        - '{value_stream}_{name}_release-clj_{qualifier}':
            dn-sort-index: ' Release-05:'
            tpb_projects: '{value_stream}_{name}_pkg-pe_{scm_branch}'

- job-group:
    name: 'clj-unit-release_postgres-integration'
    jobs:
        - '{value_stream}_{name}_unit-clj_postgres-integration_{qualifier}':
            qualifier: 'release_{scm_branch}'
            dn-qualifier: ' Pre-Release Clj/Postgres Unit/Integration Test'
            dn-sort-index: ' Release-01:'
            tpb_projects: '{value_stream}_{name}_release-clj_{scm_branch}'

        - '{value_stream}_{name}_release-clj_{qualifier}':
            dn-sort-index: ' Release-05:'
            tpb_projects: '{value_stream}_{name}_pkg-pe_{scm_branch}'
